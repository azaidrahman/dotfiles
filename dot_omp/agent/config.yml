symbolPreset: nerd
theme:
  dark: titanium
  light: light
setupVersion: 2
modelRoles:
  default: openai-codex/gpt-5.6-terra:high
# Terra caps at "high" in omp; omp rewrites ":max" to ":high" on start.
defaultThinkingLevel: high
# Model picker allowlist. Without it omp also lists its built-in catalogs
# (openai, google, ...) that have no credentials here. Only the LiteLLM proxy
# models and the Codex subscription models are usable, so show only those.
enabledModels:
  - litellm/*
  - openai-codex/gpt-5.6-terra
  - openai-codex/gpt-5.6-luna
  - openai-codex/gpt-5.5
  - openai-codex/gpt-5.4-mini
composer:
  shape: rail
# Keep startup context lean: don't auto-inject the whole Claude/Codex/Agents
# skill marketplace (~20% of the window before you type). pi-native skills stay,
# and slash-command invocation remains on for on-demand use.
skills:
  enabled: true
  enableSkillCommands: true
  enableClaudeUser: false
  enableClaudeProject: false
  enableCodexUser: false
  enableAgentsUser: false
  enableAgentsProject: false
  enablePiUser: true
  enablePiProject: true
